//! foma/flags.c — stubs only (per docs/port/rust-conventions.md).
//!
//! Called by constructions.rs (fsm_compose's flag-is-epsilon path); the real
//! port lands with the flags concern, which replaces these stubs and adds
//! the spec annotations.

pub fn flag_check(s: &str) -> i32 {
    let _ = s;
    todo!("ported by w2-flags")
}
