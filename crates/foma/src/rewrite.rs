//! foma/rewrite.c — stubs only, per the stub protocol in
//! docs/port/rust-conventions.md. Replaced (and annotated) by the
//! w2-flags-define-rewrite concern.

use crate::types::Fsmcontexts;

pub fn fsm_clear_contexts(contexts: Option<Box<Fsmcontexts>>) {
    let _ = contexts;
    todo!("ported by w2-flags-define-rewrite")
}
